# DevEco / hvigor 构建产物
.idea/
.hvigor/
.cxx/
build/
oh_modules/
**/build/
**/oh_modules/
**/package-lock.json

# 前端产物同步目标（由 npm run harmony:sync 生成；dist.README.txt 入库保留）
entry/src/main/resources/rawfile/*
!entry/src/main/resources/rawfile/dist.README.txt

# 签名材料（勿入库）
*.p12
*.csr
*.cer
*.p7b

# 含本地签名配置（密钥密文/证书路径），以 build-profile.json5.template 入库代替
build-profile.json5
