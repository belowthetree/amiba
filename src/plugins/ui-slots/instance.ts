// ============================================================
// @amiba/ui-slots — 全局 Slot 注册表单例
// ============================================================
// 插件 apply() 与服务容器都使用同一实例；
// UI 宿主在 Vue 组件中通过本模块读取，避免手动传递 loader。
// ============================================================

import { UISlotRegistry } from './registry'

export const uiSlotRegistry = new UISlotRegistry()
