{
  "apiVersion": 1,
  "id": "legacy-bootstrap",
  "kind": "plugin",
  "entry": "index.ts",
  "inject": ["storage", "settings", "toolRegistry", "toolsets", "modelProviders", "credentials", "uiShell", "router", "session", "memory", "skills", "serviceRuntime", "network", "widgets", "lifecycle"],
  "provides": {
    "services": ["app"]
  }
}
